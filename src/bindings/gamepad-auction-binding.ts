import { GamepadBinding } from "@/input";
import { AuctionRule } from "@/market";

export default function gamepadBinding(rule: AuctionRule): GamepadBinding {
    return (gamepad, player) => {
        // TODO: Write gamepad binding for auction rule
        return [];
    }
}